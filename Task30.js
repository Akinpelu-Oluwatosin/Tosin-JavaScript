const prompt = require('prompt-sync')()

let theWord = prompt("Enter a word")

let vowel = 0

for (let words = 0; i < words.length; words++) 

let letter = word[i]

if ("aeiouAEIOU".includes(letter)) 

vowels++
  
console.log("Vowels: " + vowels);